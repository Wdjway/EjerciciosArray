//Escribe un algoritmo que tome un array de números y devuelva un nuevo
//array con solo los números pares.


let number = [56, 77, 2, 4, 69, 34, 105]; 

for (i = 0; i < number.length; i++) {
//operacion 
    if (number[i] %2===0) {
        console.log(" the numbers " + number[i] + " are pars ")
        
    
    }
    
}